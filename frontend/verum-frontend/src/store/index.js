import { createStore } from "vuex";

export default createStore({
  state: {
    report_form: {
      phones: [],
      comercial_references: [{}],
    },
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
